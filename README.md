# LidOpener
macOS menu bar utility that prevents your MacBook from sleeping when the lid is closed. Includes an AI-detection mode that automatically enables clamshell mode when no user input is detected, and a timer mode. Useful for keeping AI coding assistants running in the background while your laptop lid is shut.
